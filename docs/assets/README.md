# Demo assets

Drop launch/demo media here:

- **`demo.gif`** — a 15–20 s screen capture of a vibe firing (e.g. Karren on an error): hold
  music → the character picks up → haptic/display fire. Referenced from the top-level
  [`README.md`](../../README.md) (an HTML-commented `<img>` is ready to uncomment).
- Screenshots, the AWTRIX tiles, etc.

Keep clips short and compressed — they ship in the repo. A few seconds of the *experience*
sells this faster than any prose.
